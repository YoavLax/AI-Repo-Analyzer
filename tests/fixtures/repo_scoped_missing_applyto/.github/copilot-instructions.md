# Project instructions

This service is a small Python web API.

- Use `pytest` for all test runs.
- Keep modules under `src/` small and focused.
- Prefer explicit imports over wildcard imports.

Scoped guidance lives under `.github/instructions/`.
