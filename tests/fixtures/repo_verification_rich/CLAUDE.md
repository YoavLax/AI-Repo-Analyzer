# Project Memory

## Verification

Run `pytest` after every change and iterate until they pass.
Run `make lint` before committing.
When you claim a fix works, show the output of the test run as evidence.
