apiVersion: v1
kind: Service
metadata:
  name: {{ include "agentcompass.fullname" . }}
  labels:
    {{- include "agentcompass.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "agentcompass.selectorLabels" . | nindent 4 }}
