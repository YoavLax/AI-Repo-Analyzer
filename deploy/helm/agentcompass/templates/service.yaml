apiVersion: v1
kind: Service
metadata:
  name: {{ include "codecompass.fullname" . }}
  labels:
    {{- include "codecompass.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "codecompass.selectorLabels" . | nindent 4 }}
