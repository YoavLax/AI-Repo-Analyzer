__pycache__/
*.pyc
.venv/
venv/
*.egg-info/
build/
dist/
.pytest_cache/
.mypy_cache/

# Secrets. docker-compose.yml reads GITHUB_TOKEN from here, so this file holds
# a live credential on any machine that runs the container.
.env
.env.*
!.env.example

# Personal, machine-local Claude Code files — never commit these.
CLAUDE.local.md
.claude/settings.local.json

# scripts/focus-shot.js needs playwright-core installed here.
node_modules/

# Scratch screenshots for local review — never committed. The images an
# upstream PR embeds live in docs/img/ instead.
new/
